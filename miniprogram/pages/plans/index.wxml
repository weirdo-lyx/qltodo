<view class="page">
  <view class="top-action card bear-hero">
    <view class="top-copy">
      <view class="bear-chip">布布约定小窝</view>
      <view class="cozy-title">把约定放进小窝</view>
      <view class="cozy-subtitle">长期目标也可以拆成布布一二每天愿意完成的小块。</view>
    </view>
    <view class="top-side">
      <view class="top-bears bear-frame">
        <image class="bear-art" src="/assets/bears/8acc28dfe40e7aa221dfb8cf838789e0.jpg" mode="aspectFit" />
      </view>
      <button class="btn-primary" bindtap="goCreatePlan">新建约定</button>
    </view>
  </view>

  <view wx:if="{{loading}}" class="loading-state">布布一二正在整理约定贴纸...</view>

  <view wx:elif="{{plans.length}}">
    <block wx:for="{{plans}}" wx:key="id">
      <view class="card plan-card">
        <view class="plan-top" data-id="{{item.id}}" bindtap="goDetail">
          <view class="plan-title">{{item.title}}</view>
          <view class="tag {{item.status === 'active' ? 'tag-primary' : 'tag-muted'}}">{{item.statusText}}</view>
        </view>
        <view class="plan-info" data-id="{{item.id}}" bindtap="goDetail">第 {{item.currentDay}} / {{item.totalDays}} 天</view>
        <view class="plan-info" data-id="{{item.id}}" bindtap="goDetail">已完成 {{item.completedDays}} 天</view>
        <view class="plan-info" data-id="{{item.id}}" bindtap="goDetail">开始日期：{{item.startDate}}</view>
        <view class="plan-info" data-id="{{item.id}}" bindtap="goDetail">结束日期：{{item.endDate}}</view>
        <!-- 删除功能暂时隐藏，等待后端修复 DELETE 接口 -->
        <!-- <view wx:if="{{item.status !== 'completed'}}" class="plan-actions">
          <button class="btn-danger btn-sm" data-id="{{item.id}}" catchtap="handleDeletePlan">删除</button>
        </view> -->
      </view>
    </block>
  </view>

  <view wx:else class="empty-state">
    <view class="empty-bear">
      <image class="bear-art" src="/assets/bears/72ddba9eab32e8abcb3c565df7ef0734.jpg" mode="aspectFit" />
    </view>
    <view>还没有约定，先创建一个属于你们的布布目标吧。</view>
    <button class="btn-primary empty-btn" bindtap="goCreatePlan">去创建小约定</button>
  </view>
</view>
