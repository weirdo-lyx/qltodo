Put the licensed background music file here:

- `happy-dynamic.mp3`

If you want to use a remote file instead, change `BACKGROUND_MUSIC_SRC` in
`miniprogram/utils/background-music.js` to an HTTPS URL that is allowed in the
mini program's download/file domain settings.
