// API响应类型
// export interface ApiResponse<T> { code: number; message: string; data: T; }
// export interface UserInfo { id: number; nickname: string; avatarUrl: string; totalPoints: number; hasPartner: boolean; }
// export interface LoginResult { token: string; user: any; }
// export interface MockLoginPayload { mockOpenid: string; nickname: string; avatarUrl: string; }
// export interface WxLoginPayload { code: string; nickname?: string; avatarUrl?: string; }
// export interface Plan { id: number; title: string; totalDays: number; currentDay: number; completedDays: number; status: string; startDate: string; endDate: string; }
// export interface PlanTask { id: number; title: string; points: number; checked?: boolean; checkedToday?: boolean; }
